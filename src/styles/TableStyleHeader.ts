import { SxProps, Theme } from "@mui/material";

export const TableStyleHeader: SxProps<Theme> = {
  backgroundColor: 'primary.light',
  color: 'primary.contrastText',
  position: 'sticky',
  top: 0,
  zIndex: 1
};
